{
  "name": "orbital-tetris",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Circular Tetris: blocks fall from the edges toward a living core. Rotate the core, build full rings, survive 100 levels.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "excalibur": "^0.30.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
